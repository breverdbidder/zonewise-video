{
  "name": "zonewise-video",
  "version": "2.0.0",
  "description": "ZoneWise 3-min GTM video — Revideo + light mode + storytelling",
  "scripts": {
    "start": "vite",
    "render": "npx @revideo/renderer render src/project.ts --output out/zonewise-demo.mp4",
    "render-hook": "npx @revideo/renderer render src/project.ts --output out/hook.mp4 --range 0:750",
    "render-hook-gif": "npm run render-hook && ffmpeg -i out/hook.mp4 -vf 'fps=15,scale=1280:-1:flags=lanczos,split[s0][s1];[s0]palettegen[p];[s1][p]paletteuse' out/hook.gif"
  },
  "dependencies": {
    "@revideo/2d": "^0.4.0",
    "@revideo/core": "^0.4.0"
  },
  "devDependencies": {
    "@revideo/renderer": "^0.4.0",
    "@revideo/vite-plugin": "^0.4.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
